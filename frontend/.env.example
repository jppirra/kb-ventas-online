# Backend API URL
# Local: http://localhost:8080/api
# Production (with Vercel proxy): /api
VITE_API_URL=http://localhost:8080/api

# Google OAuth Client ID (public — safe to expose in bundle)
VITE_GOOGLE_CLIENT_ID=
